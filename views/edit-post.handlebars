<div class="post-card">
    <div class="card-header">
        <h2>Edit Post</h2>
    </div>

    <form id="edit-post-form" class="card-body">
        <input type="hidden" id="post-id" value="{{post.id}}" />

        <label for="post-title" class="form-label">Title</label>
        <input type="text" class="form-input" id="post-title" value="{{post.title}}" />

        <label for="post-body" class="form-label">Content</label>
        <textarea class="form-input" id="post-body">{{post.body}}</textarea>

        <button type="submit" class="btn">Submit Edit</button>

        <button type="button" id="delete-btn" class="btn">Delete</button>
    </form>
</div>